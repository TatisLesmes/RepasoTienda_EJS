<!DOCTYPE html>
<html lang="en">

<head>
    <%-include('./templates/header.ejs')%>
        <title>Ventas</title>
</head>

<body>
    <%-include('./templates/navigation.ejs')%>

    <div class="container p-4" style="max-width: 600px;">
        <form method="POST" action="/sales" class="card card-body">


            <label for="prodcuto">Selecciona una opción:</label>
            <select class="form-select" name="producto" id="producto">
                <% Object.entries(products).forEach(([code, product])=> { %>
                <option value="<%= product.description %>"><%= product.description %></option>
              <% }); %>
            </select>

            
            <div class="form-group">
                <label for="autor">Cantidad</label>
                <input type="text" class="form-control" name="cantidad" id="cantidad" placeholder="cantidad" required>    
            </div>

            <input type="submit" value="Agregar Compra" class="btn btn-dark" >

     
        </form>
    </div>
    
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<%-include('./templates/footer.ejs')%>
</body>

</html>